name: Check CSS cache-busting acknowledgment

on:
  pull_request:
    branches:
      - main
    paths:
      - 'styles/main.css'
      - '**/*.html'

jobs:
  require-css-cache-ack:
    runs-on: ubuntu-latest

    steps:
      - name: Check PR body for CSS cache acknowledgment
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.pull_request.body || "";

            if (!body.includes("CSS-CACHE-BUSTING-ACK: YES")) {
              core.setFailed("styles/main.css changed. Add 'CSS-CACHE-BUSTING-ACK: YES' to the PR description.");
            }
